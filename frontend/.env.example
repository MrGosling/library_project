# URL API. По умолчанию используется относительный путь /api/v1,
# который проксируется nginx (production) или vite (разработка) на бэкенд.
# Переопределяйте только если бэкенд расположен на другом домене.
# VITE_API_URL=http://localhost:8000/api/v1
